{
  "name": "sls-node-graphql",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "sls deploy --aws-profile serverless-admin",
    "deploy_function": "sls deploy -f query_graphql --aws-profile serverless-admin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "graphql": "^15.1.0",
    "graphql-tools": "^6.2.6"
  },
  "devDependencies": {
    "serverless-offline": "^6.4.0"
  }
}
